<h1>ユーザー 編集</h1>
<%= form_with model: @user, local: true do |f| %>
  <p>氏名：<%= f.text_field :tille %></p>
  <p>年齢：<%= f.text_field :content %></p>
  <%= f.submit "送信" %>

<%= link_to "一覧画面へ", users_path %>
<% end %>